import * as React from 'react';
import * as ReactDOM from 'react-dom';
import Home from './views/Home';
const App = (props: any) => <Home />;

ReactDOM.render(<App />, document.getElementById('root'));
